{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "grabacion-macro",
  "description": "Capability for the macro-recording indicator overlay window (manual drag via setPosition/outerPosition, see activarArrastre en vent_grabacion_macro_main.ts)",
  "windows": ["grabacion_macro"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position",
    "core:window:allow-outer-position"
  ]
}