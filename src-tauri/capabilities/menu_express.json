{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "menu-express",
    "description": "Capability for MenuExpress floating windows (one label per open menu, glob pattern since several can be open at once)",
    "windows": ["menu_express_*"],
    "permissions": [
      "core:default",
      "core:window:allow-start-dragging"
    ]
  }