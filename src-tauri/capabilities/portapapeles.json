{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "portapapeles",
    "description": "Capability for Portapapeles floating windows (one label per open window, glob pattern since several can be open at once)",
    "windows": ["portapapeles_*"],
    "permissions": [
      "core:default",
      "core:window:allow-start-dragging"
    ]
  }