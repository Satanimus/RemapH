{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "grabacion-macro",
  "description": "Capability for the macro-recording indicator overlay window (single label, draggable via manual JS drag — no native start-dragging, ver vent_grabacion_macro_main.ts)",
  "windows": ["grabacion_macro"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position",
    "core:window:allow-scale-factor",
    "core:window:allow-outer-position"
  ]
}
